// NOTE: No function have to be mutating

// First Implement Binary tree
// Add elment as high as possible first place possble

type Tree = null

const createTree = (): Tree => null

const itemsCount = (tree: Tree) => 0

export { createTree, itemsCount}
